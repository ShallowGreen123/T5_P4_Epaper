dependencies:
  idf:
    version: ">=5.4"

  espressif/usb_host_uvc:
    version: "^2.2.0"

  espressif/esp_wifi_remote:
    version: "^1.5.0"
    rules:
      - if: "${IDF_TARGET} == esp32p4"

  espressif/esp_hosted:
    version: "^2.12.3"
    rules:
      - if: "${IDF_TARGET} == esp32p4"
